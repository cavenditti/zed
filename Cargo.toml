# Workspace declaration removed — this crate set is part of the codon workspace.
# See the root Cargo.toml for workspace configuration.

[profile.dev]
split-debuginfo = "unpacked"
incremental = true
codegen-units = 16

[profile.dev.build-override]
codegen-units = 16
split-debuginfo = "unpacked"
debug = true
